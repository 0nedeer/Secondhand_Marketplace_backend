<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.secondhand.marketplace.backend.modules.forum.mapper.ForumPostTagMapper">
    
    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.secondhand.marketplace.backend.modules.forum.entity.ForumPostTag">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="post_id" property="postId" jdbcType="BIGINT"/>
        <result column="tag_id" property="tagId" jdbcType="BIGINT"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    </resultMap>
    
    <!-- 基础字段列表 -->
    <sql id="Base_Column_List">
        id, post_id, tag_id, created_at
    </sql>
    
    <!-- 插入 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO forum_post_tag (post_id, tag_id, created_at)
        VALUES (#{postId}, #{tagId}, #{createdAt})
    </insert>
    
    <!-- 根据ID删除 -->
    <delete id="deleteById">
        DELETE FROM forum_post_tag WHERE id = #{id}
    </delete>
    
    <!-- 根据ID更新 -->
    <update id="updateById">
        UPDATE forum_post_tag
        SET post_id = #{postId},
            tag_id = #{tagId},
            created_at = #{createdAt}
        WHERE id = #{id}
    </update>
    
    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM forum_post_tag WHERE id = #{id}
    </select>
    
    <!-- 查询所有帖子标签关联 -->
    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM forum_post_tag ORDER BY id DESC
    </select>
    
    <!-- 根据帖子ID查询标签关联 -->
    <select id="selectByPostId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM forum_post_tag WHERE post_id = #{postId} ORDER BY id ASC
    </select>
    
    <!-- 根据标签ID查询帖子关联 -->
    <select id="selectByTagId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM forum_post_tag WHERE tag_id = #{tagId} ORDER BY id ASC
    </select>
    
    <!-- 根据帖子ID删除所有标签关联 -->
    <delete id="deleteByPostId">
        DELETE FROM forum_post_tag WHERE post_id = #{postId}
    </delete>
    
</mapper>