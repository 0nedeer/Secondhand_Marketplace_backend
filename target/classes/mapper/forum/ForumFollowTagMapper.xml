<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.secondhand.marketplace.backend.modules.forum.mapper.ForumFollowTagMapper">
    
    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.secondhand.marketplace.backend.modules.forum.entity.ForumFollowTag">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="tag_id" property="tagId" jdbcType="BIGINT"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    </resultMap>
    
    <!-- 基础字段列表 -->
    <sql id="Base_Column_List">
        id, user_id, tag_id, created_at
    </sql>
    
    <!-- 插入 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO forum_follow_tag (user_id, tag_id, created_at)
        VALUES (#{userId}, #{tagId}, #{createdAt})
    </insert>
    
    <!-- 根据ID删除 -->
    <delete id="deleteById">
        DELETE FROM forum_follow_tag WHERE id = #{id}
    </delete>
    
    <!-- 根据ID更新 -->
    <update id="updateById">
        UPDATE forum_follow_tag
        SET user_id = #{userId},
            tag_id = #{tagId},
            created_at = #{createdAt}
        WHERE id = #{id}
    </update>
    
    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM forum_follow_tag WHERE id = #{id}
    </select>
    
    <!-- 查询所有用户关注标签记录 -->
    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM forum_follow_tag ORDER BY created_at DESC
    </select>
    
    <!-- 根据用户ID查询关注的标签 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM forum_follow_tag
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>
    
    <!-- 根据标签ID查询关注的用户 -->
    <select id="selectByTagId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM forum_follow_tag
        WHERE tag_id = #{tagId}
        ORDER BY created_at DESC
    </select>
    
    <!-- 根据用户ID和标签ID查询关注记录 -->
    <select id="selectByUserAndTag" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM forum_follow_tag
        WHERE user_id = #{userId} AND tag_id = #{tagId}
    </select>
    
</mapper>